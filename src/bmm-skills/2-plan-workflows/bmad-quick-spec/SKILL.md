---
name: bmad-quick-spec
teammate_spawnable: true
description: Condensed story spec creation (story-spec v2 quick profile) — 6-step workflow producing a tracker issue + spec file with all 22 mandatory sections, allowing terse N/A on Real-Data Findings + External Research for internal-scope features. Parallel to bmad-create-story (14 steps full profile) for small evolutions where exhaustive investigation is overkill. Escalates to /bmad-create-story when scope exceeds quick-profile thresholds. Use when 'quick spec', 'spec rapide', 'condensed spec', 'small story', or 'spec light' is mentioned.
disable-model-invocation: true
---

Read FULLY and follow `./workflow.md` — load the file with the Read tool, do not summarise from memory, do not skip sections.
