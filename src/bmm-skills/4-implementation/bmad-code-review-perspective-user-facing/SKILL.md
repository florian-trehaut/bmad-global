---
name: bmad-code-review-perspective-user-facing
description: Single-perspective code review — User-Facing Quality (Meta-6, conditional on user-facing changes). Standalone executable subskill that performs a single-perspective UX/UI review without Agent() — designed for use as an Agent Teams teammate where the Agent tool is removed at spawn time, OR for invocation as a focused perspective check. Wraps src/bmm-skills/4-implementation/bmad-code-review/subagent-workflows/meta-6-user-facing-quality.md as a standalone workflow. Use when 'review user-facing', 'check UX', 'i18n perspective', 'accessibility perspective', 'code review perspective user-facing' is mentioned.
disable-model-invocation: true
---

Read FULLY and follow `./workflow.md` — load the file with the Read tool, do not summarise from memory, do not skip sections.
