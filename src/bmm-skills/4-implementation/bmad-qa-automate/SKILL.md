---
name: bmad-qa-automate
description: "Pragmatic test automation workflow. Detects the project's test framework, generates API tests (status codes, response structure) and E2E tests (user workflows, semantic locators) for existing features, runs them, and produces a coverage summary. Covers happy path + critical errors using standard framework APIs. Ship it and iterate. Use when 'qa automate', 'generate tests quickly', 'quick test generation', 'qa tests', 'automate qa' is mentioned."
---

Follow the instructions in ./workflow.md.
