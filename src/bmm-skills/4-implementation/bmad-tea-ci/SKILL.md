---
name: bmad-tea-ci
description: "CI/CD test pipeline setup workflow. Detects the project's test framework and CI platform, generates a production-ready pipeline config with parallel sharding, burn-in loops for flaky detection, artifact collection, and quality gates. Supports GitHub Actions and GitLab CI. Use when 'ci pipeline', 'ci cd setup', 'test pipeline', 'github actions tests', 'gitlab ci tests', 'burn-in loop', 'flaky detection' is mentioned."
---

Follow the instructions in ./workflow.md.
