---
name: bmad-upstream-sync
description: "Synchronize fork with upstream BMAD-METHOD repository. Analyzes each upstream commit individually, adapts additions to our fork's structure and conventions, and produces a clean merge. This skill should be used when the user mentions 'sync upstream', 'synchronize', 'upstream sync', 'merge upstream', 'resync with bmad', or 'pull upstream changes'."
---

Follow the instructions in ./workflow.md.
